# Quiz Game — Пользовательские сценарии

## 1. Сценарий ведущего

### 1.1. Регистрация / Авторизация
- Ведущий регистрируется или входит в личный кабинет через веб-интерфейс (логин + пароль).

### 1.2. Настройки — Бот
1. Ведущий заходит в **Настройки** в личном кабинете.
2. Вводит **токен Telegram-бота** (полученный через @BotFather).
3. Вводит **ссылку на бота** (например, `https://t.me/my_quiz_bot`).
4. Сохраняет. Бэкенд автоматически регистрирует webhook для этого бота.

### 1.3. Создание квиза
1. На дашборде нажимает «Создать квиз», вводит название.
2. В редакторе квиза создаёт **категории** (например, «География», «Наука»).
3. Внутри категорий добавляет **вопросы**. Каждый вопрос содержит:
   - Текст вопроса.
   - От **2 до 4** вариантов ответа с настраиваемым **цветом**.
   - Ровно **один** вариант помечен как правильный.
   - Опционально — **изображения** (с lightbox-просмотром во время квиза).
4. Порядок категорий и вопросов меняется **drag-n-drop**.
5. Квиз можно **экспортировать** в JSON или CSV.
6. Вопросы можно **импортировать** из JSON или CSV файла.

### 1.4. Дашборд
- Отображается список всех квизов с количеством категорий и вопросов.
- Кнопки: Редактировать, Запустить, Удалить.
- Кнопка перехода в **Историю сессий**.

### 1.5. Запуск квиза (сессия)
1. Нажимает «Запустить» на квизе.
2. Создаётся **сессия квиза** с 6-значным кодом.
3. На экране отображается:
   - **QR-код** (ссылка на бота с привязкой к сессии).
   - **6-значный код** для ручного ввода.
   - Список **подключившихся участников** (обновляется в реальном времени через WebSocket).

### 1.6. Проведение квиза
Цикл по каждому вопросу:
1. Нажимает **«Начать квиз»** / **«Следующий вопрос»**.
2. На экране: текст вопроса, категория, варианты ответов с цветами, изображения.
3. Участники отвечают через Telegram-бота. Счётчик ответов обновляется в реальном времени.
4. Нажимает **«Показать ответ»**:
   - Ответы больше не принимаются.
   - Правильный ответ подсвечивается зелёным, остальные затемняются.
   - Начисляются очки.

### 1.7. Завершение квиза
- После последнего вопроса — **таблица лидеров**: подиум (1-3 место) + таблица всех участников.
- Можно **досрочно завершить** активную сессию из Истории сессий.

### 1.8. История сессий
- Отдельная страница со списком всех сессий (пагинация по 6).
- Для каждой сессии: название квиза, код, статус, дата, количество участников.
- Кнопка **«Результаты»** — раскрывает таблицу лидеров.
- Кнопка **«Досрочное завершение»** — для активных сессий.
- Кнопка **«Открыть»** — для возврата к активной сессии.

---

## 2. Сценарий участника (Telegram-бот)

### 2.1. Первый запуск
1. Участник открывает бота (через QR-код или ссылку).
2. Команда `/start` → бот предлагает ввести **никнейм**.
3. Никнейм **сохраняется** в базе — при следующем входе вводить не нужно.

### 2.2. Главное меню
После регистрации доступны кнопки:
- **🎮 Войти в квиз** — ввести код сессии.
- **👤 Мой профиль** — просмотр и изменение ника (`/nickname Новый_ник`).
- **📊 История игр** — список завершённых игр с местом и очками.

### 2.3. Подключение к сессии
1. Участник вводит **6-значный код** или использует **deep link** из QR-кода.
2. Если ник уже сохранён — подключение автоматическое.
3. Если ник не задан — бот просит ввести его.
4. Сообщение: «Вы подключились к квизу! Ожидайте начала игры...»

### 2.4. Игровой процесс
Цикл по каждому вопросу:
1. Бот **редактирует** сообщение, показывая текст вопроса и **inline-кнопки** с вариантами.
2. Участник нажимает вариант — ответ фиксируется, кнопка помечается ✅.
3. Участник может **изменить ответ**, пока ведущий не раскрыл правильный.
4. После раскрытия — бот **редактирует** сообщение: правильно/неправильно, очки за вопрос, общий счёт.

### 2.5. Результаты
- После завершения квиза — итоговая таблица лидеров и личное место.
- Для новой игры: `/start`.

**Все обновления** во время квиза происходят через **редактирование одного сообщения** — чат не засоряется.

---

## 3. Система очков

| Компонент         | Очки                       | Кто получает        |
|-------------------|-----------------------------|---------------------|
| Правильный ответ  | **+100**                    | Только за правильный |
| Бонус за скорость | **10 — участники × 10**     | Все ответившие       |

Бонус за скорость начисляется **всем**, кто ответил, независимо от правильности.
1-й ответивший: участники × 10 очков, каждый следующий: −10, минимум: 10.
Не ответил — 0 очков.
